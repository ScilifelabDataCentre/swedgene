# Reverse proxy used only by `./scripts/dockerserve -d` (in local development).
#
# This serves the genomic data (in dir: data/) directory directly, rather than through Hugo
# As these datasets are large, otherwise Hugo builds would be very slow.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 8080;

    location /data/ {
        root /usr/share/nginx/html;
    }

    location / {
        proxy_pass http://genome-portal-hugo:1313;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
